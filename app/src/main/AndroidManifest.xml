<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.clubpossible">

    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".QrCode"
            android:exported="true" />
        <activity
            android:name=".TransferenciaFinal"
            android:exported="true" />
        <activity
            android:name=".TransferenciaFavorito"
            android:exported="true" />
        <activity android:name=".UsuariosEstablecimiento" />
        <activity android:name=".Trasferencia" />
        <activity android:name=".Tienda" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>